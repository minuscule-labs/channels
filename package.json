{
  "name": "@minu/channels-workspace",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@8.15.5",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "check": "tsc -b --pretty false",
    "test": "pnpm build && node --test packages/*/dist/test/*.test.js",
    "db:generate": "drizzle-kit generate --config packages/storage-drizzle/drizzle.config.ts",
    "relay-db:generate": "drizzle-kit generate --config packages/relay-storage-drizzle/drizzle.config.ts",
    "dev": "pnpm build && node packages/control/dist/src/review-cli.js",
    "dev:live": "pnpm --dir ../runtime build && pnpm build && node packages/control/dist/src/review-cli.js --live-pi",
    "local": "pnpm --dir ../runtime build && pnpm build && pnpm web:build && node packages/control/dist/src/local-cli.js",
    "web:dev": "pnpm --filter @minu/channels-web dev",
    "web:check": "pnpm --filter @minu/channels-web check",
    "web:test": "pnpm --filter @minu/channels-web test",
    "web:test:browser": "pnpm build && pnpm --filter @minu/channels-web test:browser",
    "web:build": "pnpm --filter @minu/channels-web build",
    "release:build": "pnpm --dir ../runtime build && pnpm build && pnpm web:build && node scripts/build-release-package.mjs",
    "release:pack": "pnpm release:build && rm -rf release/artifacts && mkdir -p release/artifacts && cd release/package && npm pack --pack-destination ../artifacts && cd ../.. && node scripts/checksum-release.mjs",
    "release:smoke": "node scripts/smoke-release-package.mjs",
    "serve": "node packages/storage-drizzle/dist/src/cli.js",
    "control": "node packages/control/dist/src/cli.js",
    "app:review": "pnpm build && node packages/control/dist/src/review-cli.js",
    "app:live": "pnpm --dir ../runtime build && pnpm build && node packages/control/dist/src/review-cli.js --live-pi"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "drizzle-kit": "^0.31.10",
    "esbuild": "^0.25.0",
    "typescript": "^5.9.0"
  }
}
